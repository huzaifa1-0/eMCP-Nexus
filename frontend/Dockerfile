# Use a lightweight web server
FROM nginx:alpine

# Copy your HTML/JS files into the server's public folder
COPY . /usr/share/nginx/html

# (Optional) Add a custom Nginx config if needed, but defaults work for simple HTML